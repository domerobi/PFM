<UserControl x:Class="PFM.Views.MaintainCalculationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PFM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="376.907">
    <UserControl.Resources>
        <local:CalculationControl x:Key="CalculationControl" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <local:CalculationControl Grid.Column="1"
                                  DataContext="{Binding}"/>
        <Button Grid.Row="1"
                Grid.Column="1"
                Content="Kalkulál" Margin="0" HorizontalAlignment="Right" VerticalAlignment="Top" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Padding="5,2" />
    </Grid>
</UserControl>
